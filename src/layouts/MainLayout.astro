---
import NavBar from "../components/NavBar.jsx";
import Footer from "../components/Footer.jsx";
import ReactRouterWrapper from "../components/ReactRouterWrapper.jsx"; // Import wrapper
const { title = "Termitrack" } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body>
    {/* Wrap NavBar with ReactRouterWrapper to provide BrowserRouter context */}
    <!-- <ReactRouterWrapper>
      <NavBar client:load onToggleModal={() => {}} />
    </ReactRouterWrapper> -->

    <main>
      <slot /> <!-- Page content will be injected here -->
    </main>

    <Footer client:load />
  </body>
</html>
